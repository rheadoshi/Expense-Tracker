<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.expenses">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Expenses"
        >

        <!-- MainActivity with intent filter -->
        <activity android:name=".MainActivity"
            android:exported="true"> <!-- Set this to true -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- DashboardActivity with no intent filter (not necessary to specify android:exported) -->
        <activity android:name=".DashboardActivity"
            android:exported="true"> <!-- Set this to true if you want it to be accessible from other apps -->
        </activity>


    </application>

</manifest>
